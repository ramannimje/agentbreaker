apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agentbreaker-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: agentbreaker.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: agentbreaker-api
                port:
                  number: 80
